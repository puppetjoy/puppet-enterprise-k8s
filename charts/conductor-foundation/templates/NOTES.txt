Conductor foundation deployed.

Hub service:
  {{ include "conductor.hubServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.hub.service.amqpPort }}

Hub management API:
  http://{{ include "conductor.hubServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.hub.service.managementPort }}

Warden publishes per-participant onboarding bundles into Secrets labeled:
  pe-k8s.puppet.com/conductor=onboarding-bundle

Warden also assembles segment trust bundles into Secrets labeled:
  pe-k8s.puppet.com/conductor=trust-bundle

{{- if .Values.cassandra.enabled }}

Cassandra shared-state service:
  {{ include "conductor.cassandraServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.cassandra.service.cqlPort }}
{{- end }}
