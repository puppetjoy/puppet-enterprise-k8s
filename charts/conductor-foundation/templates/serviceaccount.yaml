{{- if and .Values.warden.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "conductor.serviceAccountName" . }}
  labels:
{{ include "conductor.labels" . | indent 4 }}
{{- end }}
