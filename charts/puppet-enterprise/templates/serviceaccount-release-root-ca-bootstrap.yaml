{{- if eq .Values.controlPlane.ca.provider "releaseRoot" }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pe.fullname" . }}-control-plane-ca-bootstrap
  labels:
    {{- include "pe.labels" . | nindent 4 }}
{{- end }}
